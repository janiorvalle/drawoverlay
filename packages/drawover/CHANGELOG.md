# drawoverlay

## 0.1.0

### Minor Changes

- 60bdef9: First public release. Element comments with precise selectors and
  accessible facts, drawing tools (rectangles, ellipses, arrows, lines,
  text, images), one-press Markdown and annotated-PNG copy built for
  pasting into coding agents, capture that inlines the assets your page
  already shows, and localStorage persistence — all inside an open Shadow
  DOM with zero runtime network activity at rest.
